# Protocol examples

See `packages/protocol/fixtures/valid.json` for schema-validation examples, including an engine-free tabletop project. These are test fixtures, not Studio production state. Cosmic Meltdown identity belongs in its own project protocol during Phase 6.
